#include<stdio.h>
#include<math.h>
int main(){
    float n;
    printf("enter number to take square root");
    scanf("%f",&n);
    float root=sqrt(n);
    printf("root of n is %f",root);
    return 0;
}
#include<stdio.h>
#include<string.h>

int main()
{
   int n;
   printf("enter a number");
   scanf("%d",&n);
   if(n%2==0);
   printf("the number is an even number");
   else printf("the number is an odd number");
    return 0;
}